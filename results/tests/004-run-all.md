The script works correctly — it found both tests, ran them, captured their exit codes, printed the summary, and exited 1 because both tests failed (environment issues unrelated to the runner).

`src/tests/run-all.sh` is created and executable.
