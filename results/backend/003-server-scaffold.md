Done. `src/backend/server.js` created and committed with:
- CORS enabled for all origins
- `GET /health` → `{ status, timestamp }`
- `GET /api/hello/:name` → `{ message }`
- Listens on `0.0.0.0:3002`
