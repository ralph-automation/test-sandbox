Create `src/backend/hello.js` with:

```js
function hello(name) {
  return `Hello, ${name}! Backend is alive.`;
}

module.exports = { hello };
```

Create exactly this file. Do not modify any other files.