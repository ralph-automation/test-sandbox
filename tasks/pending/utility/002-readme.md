Create `docs/README.md` with a brief project overview:

```markdown
# Test Sandbox

A test project for the Ralph Loops orchestration engine.

## Structure

- `src/backend/` — Backend modules
- `src/frontend/` — Frontend pages and styles
- `src/tests/` — Test scripts
- `docs/` — Documentation
- `tasks/` — Task queue (pending/in-progress/done)
```

Create exactly this file. Do not modify any other files.