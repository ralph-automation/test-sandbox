Create a file called `proof-of-life.txt` in the project root directory containing exactly:

```
ROLE agent alive
timestamp: <current date and time>
hostname: <machine hostname>
```

Replace ROLE with your actual role name and the placeholders with actual values. Do not create any other files.
