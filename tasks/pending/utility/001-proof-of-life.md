Create a file called `proof-of-life-utility.txt` in the project root containing:

```
utility agent alive
timestamp: <current date and time>
hostname: <machine hostname>
```

Replace placeholders with actual values. Do not create any other files.