Create `src/tests/health.sh` — a bash script that:

1. Prints "=== Health Check ==="
2. Checks if `src/backend/server.js` exists, prints PASS or FAIL
3. Checks if `src/frontend/index.html` exists, prints PASS or FAIL
4. Checks if `src/frontend/styles.css` exists, prints PASS or FAIL
5. Exits 0 if all pass, 1 if any fail

Make the script executable. Do not modify any other files.
